1) nasmerujte v prikazovem radku do slozky obsahujici algoritmus.js
2) zadejte: node algoritmus.js --pravidlo "(prvek) => prvek < 20"   - tento kod vyfiltruje vsechny prvky mensi nez 20
3) podminku zobrazeni muzete libovolne zmenit (prvek > 20, prvek == 20)